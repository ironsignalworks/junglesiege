/* ...existing code... */

/* HUD Top Bar & Bottom Bar - CONSISTENT GREY BOX STYLE (removed yellow lines) */
.hud-top,
.hud-bottom {
  /* shared placement handled by each rule, visuals unified here */
  background: linear-gradient(180deg,#2f2f2f,#111) !important;
  border: 1px solid #444 !important; /* neutral grey border, removed yellow */
  border-radius: 6px !important;
  box-shadow: inset 0 1px 0 rgba(255,255,255,0.03), 0 3px 6px rgba(0,0,0,0.6);
  color: var(--primary-green) !important; /* keep readable value color */
  font-family: 'Press Start 2P', monospace !important;
}

/* Top bar placement & sizing */
.hud-top {
  position: absolute;
  top: 8px;
  left: 8px;
  right: 8px;
  height: 64px; /* doubled */
  padding: 12px 16px;
  display: flex;
  justify-content: space-between;
  align-items: center;
  gap: 12px;
}

.hud-top .cluster { display:flex; gap:12px; align-items:center; }
.hud-top .tag {
  display:flex;
  flex-direction:column;
  align-items:center;
  justify-content:center;
  gap:6px;
  padding:8px 12px;
  min-width:120px;
  background: transparent; /* use unified background from .hud-top */
  border: none; /* inner tiles visually flush */
  border-radius: 4px;
  box-shadow: none;
}
.hud-top .lbl, .hud-label { color: var(--primary-green) !important; font-size:12px !important; text-align:center; }
.hud-top .val, .hud-value { color: var(--primary-green) !important; font-size:18px !important; font-weight:bold; text-align:center; }

/* Bottom bar placement & sizing */
.hud-bottom {
  position: absolute !important;
  bottom: 8px !important;
  left: 8px !important;
  right: 8px !important;
  height: 64px !important; /* doubled */
  padding: 12px 16px !important;
  display:flex !important;
  justify-content:space-between !important;
  align-items:center !important;
  gap:16px !important;
}

.hud-seg {
  display:flex !important;
  flex-direction:column !important;
  align-items:center !important;
  justify-content:center !important;
  gap:8px !important;
  padding:8px 14px !important;
  min-width:140px;
  background: transparent; /* unified */
  border: none !important;
  border-radius: 4px !important;
  box-shadow: none !important;
}

/* HP bar and pips sizing retained for new tiles */
.hp-bar { height:28px !important; max-width:220px !important; border:1px solid #333 !important; background: linear-gradient(180deg,#1d0a0a,#2b0a0a) !important; }
.hp-fill { left:2px !important; top:2px !important; bottom:2px !important; }
.pips { gap:8px !important; }
.pip { width:12px !important; height:14px !important; border:1px solid #555 !important; }
.pip.on { border-color: #888 !important; }

/* Remove any yellow-specific rules for HUD top/bottom to avoid stray yellow lines */
.hud-top, .hud-bottom, .hud-top .tag, .hud-seg, .hud-top .lbl, .hud-top .val, .hud-label, .hud-value {
  /* override yellow variables locally */
  --hud-border: #444;
  color: var(--primary-green) !important;
}

/* Mobile fallback */
@media (max-width:600px),(max-height:450px) {
  .hud-top, .hud-bottom { height:48px !important; padding:8px 10px !important; }
  .hud-top .tag, .hud-seg { min-width:80px; padding:6px 8px; }
  .hud-top .lbl, .hud-label { font-size:10px !important; }
  .hud-top .val, .hud-value { font-size:14px !important; }
  .hp-bar { height:20px !important; max-width:160px !important; }
  .pip { width:10px !important; height:10px !important; }
}

/* ...existing code... */